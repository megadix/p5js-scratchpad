import Sketch from "../../common/Sketch";
import StarfieldScript from "./StarfieldScript";

export default function StarfieldPage() {
  return (
    <>
      <p>Don't forget to try Warp Speeed ;)</p>
      <Sketch sketch={StarfieldScript} />
    </>
  );
}